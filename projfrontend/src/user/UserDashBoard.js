import React from "react";
import Base from "../core/Base";

const UserDashboard = () => {
    return (
        <Base title="User Dashboard page" description="This is User Page!!!">
            <h1>User Dashboard</h1>
        </Base>
    )
};

export default UserDashboard;
